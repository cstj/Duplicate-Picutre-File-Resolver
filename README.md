Duplicate-Picutre-File-Resolver
===============================

Finds duplicates files (by length and the contents using hashs) within a directory.  Then you can deal with the how you want (currenly delete files or keep specific one).
